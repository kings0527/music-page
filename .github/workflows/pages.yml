name: Deploy music player to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository and LFS audio
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Validate player and playlist
        run: |
          python3 -m unittest -v tests/test_site.py
          node --test tests/*.test.mjs

      - name: Prepare static site
        run: |
          mkdir -p _site/audio _site/lyrics
          cp index.html styles.css app.js player-logic.js lyrics-timeline.js cache-client.js cache-logic.js service-worker.js tracks.json _site/
          cp -R audio/. _site/audio/
          cp -R lyrics/. _site/lyrics/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
